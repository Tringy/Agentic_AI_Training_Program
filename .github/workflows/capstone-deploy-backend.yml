name: Deploy Capstone Backend to Fly.io

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'labs/capstone-options/option-e-football-review/python/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Fly CLI
        uses: superfly/flyctl-actions/setup-flyctl@master

      - name: Deploy to Fly.io
        env:
          FLY_API_TOKEN: ${{ secrets.CAPSTONE_FOOTBALL_REVIEW_API_FLY_API_TOKEN }}
        run: flyctl deploy --remote-only --config fly.toml
        working-directory: ./labs/capstone-options/option-e-football-review/python
